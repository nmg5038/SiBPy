
#----------------------------------------------------------------------
#
#   SiB4 Pool Informational Module
#
#----------------------------------------------------------------------


pool_name_long = None # pool names
pool_name = None      # short pool names
pool_type = None     # pool types (live, dead)
pool_loc = None      # pool locations (soil, surface, canopy)
pool_indx_lay = None # index for lowest soil layer with carbon

#...Pool indices

pool_indx_can = None  # pool index numbers for all canopy pools
pool_indx_sfc = None  # pool index numbers for all surface pools
pool_indx_soil = None    # pool index numbers for all soil pools

pool_indx_leaf = None   # pool index number for leaf pool
pool_indx_froot = None  # pool index number for fine root pool
pool_indx_croot = None   # pool index number for coarse root pool
pool_indx_stwd = None   # pool index number for stem/wood pool
pool_indx_prod = None   # pool index number for product pool
pool_indx_cdb = None    # pool index number for coarse dead biomass pool
pool_indx_metl = None   # pool index number for metabolic litter pool
pool_indx_strl = None   # pool index number for structural litter pool
pool_indx_slit = None   # pool index number for soil litter pool
pool_indx_slow = None   # pool index number for soil slow pool
pool_indx_arm   = None  # pool index number for soil armored pool
